#include <iostream>
#include <math.h>
using namespace std;
int main() {
  double a, b, x;
  cout <<"Введіть число a і b: "; cin >> a >> b;
  x=pow(a,b);
  cout <<"Результат: " << x << endl;
  return 0;
}